{
  "name": "arch-exam-drill",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run prisma:generate -w packages/db && npm run build -w apps/web",
    "dev": "npm run prisma:generate -w packages/db && npm run dev -w apps/web",
    "lint": "npm run lint -w apps/web",
    "test": "npm test -w apps/web",
    "prisma:generate": "npm run prisma:generate -w packages/db",
    "db:seed": "pnpm --dir packages/db seed"
  },
  "dependencies": {}
}
